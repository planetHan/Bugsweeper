int main(){
    // get mine num

    Game = new Game(totalMines, x, y);

    while (openCells == 0){
        //get command
        if (command = open){
            PlaceMines()
            break;
        }
    }

    while (Game.IsOver()){
        //get command
    }

    

    // while get commands

    // end
}